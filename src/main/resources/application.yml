server:
  port: 8081
spring:
  application:
    name: SnapMsg
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/snap_msg?serverTimezone=UTC
    username: root
    password: ''
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format-sql: false
